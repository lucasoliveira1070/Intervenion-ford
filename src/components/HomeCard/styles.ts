import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
    cardTitle: {
        fontSize: 18,
        fontWeight: "bold",
        marginTop: 15,
        marginLeft: 15
    },
    dailyProgressCard: {
        backgroundColor: "#333333",
        flex: 0.9,
        borderRadius: 15,
        marginTop: 20

    },
});