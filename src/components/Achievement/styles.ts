import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
    card: {
        backgroundColor: '#333333',
        padding: 10,
        borderRadius: 16,
        marginTop: 15,
        borderWidth: 1.5
    },
    cardAchievementAcquired: {
        borderColor: 'green'
    },
    cardTitle: {
        color: 'white',
        marginLeft: 10,
        marginTop: 5,
        fontSize: 16,
        fontWeight: 'bold'
    }
});